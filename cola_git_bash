# configurar (só 1x)
git config --global user.name "Priscila Ribeiro"
git config --global user.email "priscila@example.com"

# clonar
git clone https://github.com/usuario/meu-repo.git
cd meu-repo

# editar no VSCode
code .

# no terminal, antes de commitar
git status
git add .
git commit -m "Corrige bug X e adiciona Y"
git pull --rebase origin main
git push origin main

# quando quiser encerrar/remover credenciais (Windows exemplo)
# abrir Gerenciador de Credenciais e remover github.com
